# notebooks

Jupyter notebooks for exploration, prototyping, and figure generation.

_No notebooks yet._ Reusable logic that graduates out of a notebook should be
moved into [`../scripts/`](../scripts/) so it can be imported and version-tracked cleanly.

Note: notebook checkpoints (`.ipynb_checkpoints/`) are gitignored.
